interface SkipsProps {
  [key: string]: string;
}

export const Skips: SkipsProps = {
  eigengrau:
    'Skipped due to a migration error. Start `provenanced` with the `--unsafe-skip-upgrades=3417970` to bypass',
};
